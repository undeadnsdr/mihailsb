import { works } from '@/lib/content'
import { AvitoShot, BrowserShot } from '@/components/avito/avito-shot'

/**
 * Кадр 3 — доказательство крупным планом, первый из трёх однотипных
 * (кровля → отопление → септики). Однотипных сознательно: в галерее
 * объявления они листаются подряд, и одинаковая рамка заставляет
 * сравнивать содержимое сайтов, а не разглядывать разное оформление
 * кадров.
 *
 * Видна ровно верхняя часть страницы — та, по которой заказчик решает,
 * звонить или закрыть. Ниже первого экрана окно обрезает: «вся страница
 * мелко» не читается ни в ленте, ни в галерее.
 */
const work = works.find((item) => item.id === 'roof') ?? works[0]

export default function Avito3() {
  return (
    <AvitoShot
      title="Кровля и фасады"
      sub="Цена за квадрат и кнопка звонка — сразу, без прокрутки."
      note="Тюмень и область"
    >
      <BrowserShot work={work} priority className="w-[102cqmin]" />
    </AvitoShot>
  )
}
